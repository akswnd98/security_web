<DB의 정보를 확인>
	
	원하는 값만 출력하도록 하는 blind sql injection: 
		1 union select 1, COLUMN_NAME, 2 from information_schema.COLUMNS limit 1, 1

	상황이 극단적으로 변해 php어플리케이션이 DB내의 사용자 정보를 리턴하지 않는 다면?
	
	그래도 참 거짓 정도 확인이 가능하다.
	그리고
		1. 알아내고자 하는 데이터를 얻어온다.
			- 한줄에 한 컬럼만 가능.

		2. 데이터에서 한 글자씩 뽑아온다.
		3. 참인 값이 나오면 해당 글자를 확인.
		4. substr('korea', 2, 1)는 korea의 2번째 1글자를 가져옴.
		5. substr((<select query>), <num1>, <num2>)를 사용하면 쿼리의 리턴을 문자열로 사용가능하다.
		6. substr함수에 첫번째 인자에 쿼리를 넣을 수도 있는데, 쿼리의 리턴은 1 COLUMN 1 ROT여야만 한다.
		7. 글자를 한글자씩 가져오고 그 글자를 임의로 비교하여 참이 나오면 그 글자임.
		8. 웹브라우저가 문자값을 잘 못 전달하므로 쿼리 앞에 ascii함수를 붙여서 아스키 코드 값으로 비교를 한다.

	더 상황이 극단적으로 변해 php어플리케이션이 아예 변화 즉 참 거짓을 확인 할 수 없다면?
	
	변화를 만들어 쓴다.
		1. sleep(10)같은 함수를 쓴다.

</DB의 정보를 확인>